# Operator notes — Bexar, Texas

Generated by `scaffold/bootstrap_county.py` on 2026-05-14 15:51:49 UTC.
Framework version: v5.1.1-beta

This file captures operator-volunteered knowledge during Phase 0 that does
NOT rise to the level of a schema-recorded operator override. It is a
contextual companion to `config/counties/bexar_tx.json`, not a
schema-validated artifact.

See MASTER_PROMPT.md Section 4.30 for the rule. Two channels with a clean
separation of concerns:

- **`operator_override_audit`** (in the county config JSON) — records
  every operator override that CHANGES what the framework is allowed
  to do.
- **`operator_notes.md`** (this file) — records CASUAL operator
  knowledge that doesn't change framework behavior but is worth
  keeping for context.

Claude Code MUST append to this file during Phase 0 as operator
knowledge surfaces. Each entry should be:

- Timestamped with ISO 8601 UTC
- Under a section heading for the relevant source ID (or `## general`)
- Briefly summarized in Claude Code's own words

## general

- **2026-05-14T15:55:00Z (recon observation):** Bexar County (San Antonio metro) is the 4th-most-populous Texas county. State-mandated non-judicial foreclosure — trustee sales the first Tuesday of each month at the west side of the Bexar County Courthouse (between Courthouse and Paul Elizondo Tower); first-Wednesday fallback when January 1 or July 4 falls on a Tuesday. The County Clerk records ALL trustee sale notices in the recorded-instrument index AND publishes them on a public foreclosure map at `https://maps.bexar.org/foreclosures` with CSV export — this map is the easiest path to daily-fresh distress leads and should be the MVP source.

## clerk_recordings

- **2026-05-14T15:55:00Z (recon observation):** The County Clerk's Official Public Records search is at `https://bexar.tx.publicsearch.us/` (PublicSearch / Neumo, Kofile family). Quick Search is publicly accessible with date-range filters and grantor/grantee/doc-type/doc-number search. Login is optional ("Sign In" is offered but not required). Document image access (full PDF retrieval) cost not confirmed — typical pattern for this vendor family is paid-per-image or subscription for document downloads while index search is free. Phase 0.5 should not be triggered: index-level access satisfies the build's foreclosure-notice detection needs (a recorded Notice of Trustee Sale event with date + doc# + parties is itself the lead), but operator should confirm document-image policy before Phase 2.

## foreclosure_notices_map

- **2026-05-14T15:55:00Z (recon observation):** `https://maps.bexar.org/foreclosures` is a Bexar County Clerk-published live map of the *current month's* mortgage AND tax foreclosure notices. Free, no login. Exposes a data table view, CSV export, and a downloadable PDF of the full list. Links out to document PDFs. This is the strongest single P0 source for daily-fresh distress in Bexar — it's both authoritative (County Clerk publishes it) and operationally easy (CSV export removes any portal-scraping headache). Recommend MVP build off this source first.

- **2026-05-14T16:53:00Z (Phase 2 build):** ArcGIS REST endpoint located at `https://maps.bexar.org/arcgis/rest/services/CC/ForeclosuresProd/MapServer` (CC folder = County Clerk). Two layers: `0/Mortgage` and `1/Tax`. Both layers share the same 10-field schema: `OBJECTID, ADDRESS, DOC_NUMBER, YEAR, MONTH, SCHOOL_DIST, TYPE, CITY, ZIP, Shape`. `maxRecordCount: 1000`. Pagination via `resultOffset`. First live pull (2026-05-14T16:53:00Z) returned **288 records** (273 mortgage + 15 tax). Distribution: 163 records dated June 2026, 111 dated May 2026, 12 from April 2026, 1 from Feb 2026, 1 from Dec 2025 (these last carry-overs are likely postponed sales or notices recorded earlier that remain active). All records carry DOC_NUMBER and ADDRESS — parser_confidence 95 across the board. The `TYPE` field is coarse (just "MORTGAGE" or "TAX") — the specific instrument doc-type (Notice of Substitute Trustee's Sale, Notice of Trustee's Sale, etc.) lives in the actual recorded document at `bexar.tx.publicsearch.us`, accessible by joining on DOC_NUMBER. Geocode geometry uses NAD83 / Texas South Central (FIPS 4204) — wkid 102739 / 2278 — points are in feet. Top-10 cities: San Antonio (253), Converse (15), Boerne (3), then 1-2 each across Adkins, Helotes, Saint Hedwig, Universal City, Von Ormy, Live Oak, Somerset.

- **2026-05-14T16:53:00Z (operator dependency):** Notice that `Boerne` is in Kendall County, not Bexar — some addresses with San Antonio-metro ZIPs get attributed to neighboring municipalities by the Bexar Clerk if the property's incorporation overlaps. The 3 Boerne records should be verified against parcel ownership before treating them as Bexar-jurisdictional leads. Possible municipality misclassification at the county boundary.

## court_civil

- **2026-05-14T15:55:00Z (recon observation):** The OLD `https://search.bexar.org/` and `https://apps.bexar.org/dklitsearch/search.aspx` are deprecated. As of an undated migration, all civil district court records moved to the Justice Information Portal on Tyler Technologies Odyssey at `https://portal-txbexar.tylertech.cloud/Portal/`. Registration is NOT required for public search. The old `apps.bexar.org/dklitsearch/search.aspx` page now displays a notice ("civil case records will no longer be available through this system") and points to the Tyler portal. Probate, criminal, and dockets are all consolidated into the same Tyler portal.

## tax_collector

- **2026-05-14T15:55:00Z (recon observation):** Bexar's Tax Assessor-Collector account search at `https://bexar.acttax.com/act_webdev/bexar/index.jsp` is powered by ACT Tax Solutions (vendor). Free public account search by owner / business / address / account number / CAD property ID / fiduciary number. The portal does not expose a separate "delinquent accounts" list; delinquent status is visible at the per-account level. To get a delinquency roll, the operator may need to do a Public Information Act request OR pull every parcel from BCAD and walk each into the ACT portal.

## court_eviction

- **2026-05-14T15:55:00Z (recon observation):** Bexar County has four JP Precincts that each handle eviction (Forcible Detainer) filings. There is no centralized JP eviction search at the county level — each precinct may publish its own portal. Phase 0 did not locate a unified JP search; this is a follow-up item for either Phase 0.5 or an operator decision (walk each precinct individually, or accept that eviction leads will not be in this build's first slice). Eviction is excluded from the v1 source map until a verified portal is located.

## code_enforcement_sa

- **2026-05-14T15:55:00Z (recon observation):** City of San Antonio (not Bexar County) Code Enforcement complaint status at `https://webapp1.sanantonio.gov/CodeComplaintStatus/`. Free public search by address / complaint number / date / category. Statuses: Open / Pending / Closed. Categories include vacant lots, dangerous premises, junked vehicles, minimum housing violations, zoning, dumping, graffiti. Coverage is SAN ANTONIO CITY LIMITS ONLY — does not cover unincorporated Bexar County or the 25 other incorporated municipalities. Bexar County Fire Marshal handles code in unincorporated areas; no public search portal located for that scope. Demolition/condemnation orders are not separately exposed.

## parcel_master

- **2026-05-14T15:55:00Z (recon observation):** Bexar Central Appraisal District (BCAD) is in active migration from True Automation (legacy: `https://bexar.trueautomation.com/clientdb/?cid=110`) to Harris Govern / Aumentum (new: `https://hgo.harrisgovern.com/bexar/property/search`). Both portals are still live as of recon. BCAD does not advertise a bulk parcel download on its public site; the GIS open-data ArcGIS layer at `https://maps.bexar.org/arcgis/rest/services/Parcels/MapServer/0` is the practical bulk pull point for parcel geometry + identifiers. For full appraisal fields (assessed value, exemptions, owner mailing), per-parcel lookup via Harris Govern is the working pattern. A bulk-roll request is a separate operator workflow.

- **2026-05-14T18:35:00Z (Phase 4 access-pattern decision):** Updated finding — the `maps.bexar.org/arcgis/rest/services/Parcels/MapServer/0` layer carries the FULL appraisal field set, not just geometry. Verified fields: `PropID` (BCAD parcel id), `Situs`, `Owner` (LAST FIRST format), `AddrLn1`-`AddrLn3` (mailing address continuation lines for multi-line entity names + ATTN markers), `AddrCity`/`AddrSt`/`Zip`/`Zip4` (mailing geography), `LandVal`/`ImprVal`/`TotVal` (separate land/improvement/total appraised values), `YrBlt` (string year or "NULL"), `Exempts` (e.g. `"HS, OV65"` for homestead + over-65), `PropUse` (property class code), `LglAcres`/`Acres`, `LglDesc`. Layer accepts standard ArcGIS WHERE clauses including LIKE. `maxRecordCount: 1000` with pagination via `resultOffset` + `exceededTransferLimit`. Bulk pull is the right access pattern — no Harris Govern SPA automation needed for Phase 4.

- **2026-05-14T18:50:00Z (operator-volunteered owner-name pattern spec, post-REVIEW_GATE_4):** The operator's mental model treats parcel data as ENRICHMENT (P2), NOT a primary lead source — Phase 0 verdict stands. But owner-name strings on the parcel master ARE a primary signal source when they match specific patterns. Three patterns matter:

  | pattern | regex | canonical | confidence | operator rationale |
  |---|---|---|---|---|
  | estate | `\b(ESTATE OF\|EST OF\|ESTATE\|HEIRS OF\|HEIRS)\b` | `estate` pattern, new ESTATE_OWNER_NAME_PATTERN doc type | 75 | Heir-hunting before court probate filings catch up — appraisal district sees the estate string at owner-of-record change. |
  | living trust | `\b(LIVING TRUST\|FAMILY TRUST\|REVOCABLE TRUST\|REV TRUST\|TRUST\|TRUSTEE)\b` | `transfer` pattern, new LIVING_TRUST_OWNER_NAME_PATTERN doc type | 70 | Trust ownership often signals deceased/incapacitated original owner; investor leverage when paired with distress. |
  | entity | `\b(LLC\|INC\|CORP\|LP\|LTD\|COMPANY\|HOLDINGS)\b` | existing `entity_owned` attribute | 95 | Already framework-canonical as an attribute. |

  Implementation:
  - Estate + living-trust patterns become NEW signals (not just attributes) so they stack onto leads as additional patterns and bump the score via stack-depth bonus.
  - Foreclosure + estate-name = depth-2 stack, score jumps from Strong (~65) into Hot (~80+) — operator-callable lead.
  - The `entity_owned` regex in `scaffold/pipeline/normalize.py` tightens to the operator's set (drops TRUST/TR/TRUSTEE which now fire LIVING_TRUST signals, and drops CO/ASSOC/PARTNERS as too broad).
  - Framework gap (canonical_doc_types.json doesn't declare these signal classes) logged in `runs/bexar_tx/backlog/v5.1.2-beta-framework-patches.md` for the next framework release. Bexar build implements them via the synthetic CANONICAL.setdefault path; the framework patch is for cross-county durability.

- **2026-05-14T18:35:00Z (data shape observations):**
  - **Situs format uses DOUBLE-SPACES** between house number and street name plus a trailing space, e.g. `"3795  MOUNT OLIVE RD "`. WHERE clauses must accommodate this or normalize to single-space before matching.
  - **Multi-parcel addresses exist.** 3795 Mount Olive Rd, Adkins returns 2 distinct BCAD parcels (PropID 330032 owned by WORTHY HELEN L, 6.386 acres, HS+OV65 exemptions, YrBlt 1993; PropID 1160155 owned by BRANDT KENNETH R & JENNIFER L, 5.2 acres, no exemptions, YrBlt 2009). The foreclosure record's DOC_NUMBER would resolve the ambiguity via the recorded instrument's legal description. For Phase 4, flag multi-match as a review case.
  - **Boerne is appraised by BCAD.** ZIP 78015 (Boerne) records exist in the Parcels layer with HS exemptions and typical residential PropUse codes. The Boerne records previously flagged `potential_cross_county_leak` ARE within BCAD's appraisal coverage — meaning the city/county-boundary case is more nuanced than "wrong county." Keep the flag for operator review semantics but reframe its meaning as "review whether this is a Bexar lead" rather than "drop because it's not Bexar."
  - **`last_sale_date` / `last_sale_price` NOT present** in this layer. These live in BCAD's sales-history dataset which is reachable via Harris Govern per-parcel (or via a paid bulk roll). Phase 4 will NOT populate these fields; `long_term_owned` and `high_equity` attribute derivations will be empty until a follow-up source wires them in. Honest empty buckets per framework spirit.
  - **Address-token fallback needed.** Foreclosure record addresses don't always literally match BCAD's Situs. Example: foreclosure says "13926 WOOL PARK" in 78247; BCAD's 13926 entries in 78247 are MOONCREST / GROVE PATCH / TREE CROSSING — no WOOL PARK. This could be a foreclosure data typo OR a recorded street alias. Matcher must support house# + street-token fallback with `address_match_uncertain` review_flag when fuzzy match fires.

---

- **2026-05-15 (v5.1.2-beta-r3 migration — Step 5'' wrap transform):** Ran a one-time deterministic shape transform on `data/raw/parcel_master.jsonl`. The pre-r2 Bexar scraper wrote flat records (no `raw_payload` wrapper); the v5.1.2-beta-r2 canonical translator contract requires the §4.32 wrapped envelope. Transform script wrapped each of 316 flat records into `{"raw_record_id": "raw_<parcel_id>", "source_id": "parcel_master", "source_url": "...", "source_fetched_at": <_fetched_at>, "parser_confidence": 100, "raw_payload": <original flat record>}`. Record content is bit-identical inside the wrapper; lead-count baseline is preserved.

  Paired with the wrap: a per-source `field_map` in `config/counties/bexar_tx.json` `sources.parcel_master.field_map` bridges Bexar's scraper field names to canonical r3 translator names — `address` ← `situs_address`, `city` ← `situs_city`, `zip` ← `situs_zip`, `owner_mailing_address` ← `owner_mailing_addr1`, `property_use` ← `property_class`. The other parcel fields (`parcel_id`, `owner_name`, `exempt_homestead`, `exempt_over_65`, `exempt_disabled`, `assessed_value`, `land_value`, `improvement_value`, `year_built`, `acres`, `legal_description`) match identity and need no mapping. `exempt_veteran` defaults False — Bexar's scraper does not pre-parse it; the data's `exemptions: "DVHS, HS"` string is not re-parsed (translator boolean fast-path is triggered by presence of `exempt_homestead`).

  Foreclosure scraper output (`data/raw/foreclosure_notices_map.jsonl`) needs no transform and no field_map — it already conforms to §4.32 with canonical field names.

---

- **2026-05-15 (v5.1.2-beta migration complete):** All 12 playbook steps executed against framework commit 6bb8796 (v5.1.2-beta-r3 canonical, with two prior r1→r2→r3 framework patches surfaced from migration and shipped upstream first). Strict output equivalence verified against `data/leads.baseline.json` on all four required metrics:

  - **lead_total:** 287 (baseline 287)
  - **pattern_counts:** {foreclosure: 273, tax: 15}
  - **attribute_counts:** {absentee: 24, entity_owned: 3, senior_owner: 26}
  - **score_tier_distribution:** {Strong: 258, Workable: 29}
  - **stack_depth_distribution:** {1: 286, 2: 1}

  Test-result summary (canonical gate suite + Bexar regression tests):
  - Golden path: 46/46 PASS
  - Atomic county config writer: 18/18 PASS
  - Translator registry: 56/56 PASS (up from 26/26 in r1 after r2/r3 expansions)
  - test_owner_name_patterns: 28/28 PASS (added one defensive-guard test)
  - test_matcher: 27/27 PASS
  - test_owner_name_signal_integration: 21/21 PASS
  - verify_synthetic_harness: 110/110 PASS
  - test_foreclosure_notices_map: 9/9 PASS
  - County-agnostic regression: FAIL (58 violations — 9 universal US state codes + 49 Bexar-side test-fixture references). Accepted as known framework-scanner gap. Pipeline code (`scaffold/pipeline/*.py`) is contamination-free; remaining violations are scanner false-positives on the `_VALID_US_STATE_CODES` frozenset and Bexar-specific test files. Backlog captured in `runs/bexar_tx/backlog/v5.1.2-beta-framework-patches.md` for v5.1.2-beta-final.

  One-time data shape transform applied to `data/raw/parcel_master.jsonl` (Step 5'' wrap script — 316 records preserved, bit-identical inside `raw_payload` envelope). Per-source `field_map` declared in `config/counties/bexar_tx.json` to bridge Bexar scraper field names (`situs_address`/`situs_city`/`situs_zip`/`owner_mailing_addr1`/`property_class`) to canonical r3 translator names. `scrapers/` directory untouched (out of playbook scope).

---

## 2026-05-15 — Bexar PublicSearch access classification

Operator verified that bexar.tx.publicsearch.us does not require login or paid subscription to run searches, view result lists, open detail pages, read index metadata, or view document information needed for lead signal extraction. PDF download may require payment, login, or a separate download flow, but Phase 5 does not depend on PDF downloads.

---

## 2026-05-28 — Build Mode: clerk_recordings scraper built (locked v2 spec)

- **2026-05-28 (Build Mode increment):** Built `scrapers/clerk_recordings.py` from the operator-approved v2 scraper spec (`recon/publicsearch_clerk_recordings_scraper_spec.md`, all 5 open questions resolved 2026-05-17). Faithful to the spec: daily_refresh (default) + first_run_backfill modes, historical_lookup/unknown rejected; 3-day overlap cursor with 30-day first-run fallback; CORE-then-EXPANDED alphabetical iteration of the 20 daily-refresh codes; header-driven `td.col-N` extraction of all 11 raw_payload fields; §4.32-wrapped output appended to `data/raw/clerk_recordings.jsonl`; per-run metadata + state cursor + 30-day-rotated raw-HTML audit; retry/backoff [5,30,120] then skip; hard-halt on login/CAPTCHA/maintenance with conservative cursor freeze (CORE failure or ≥3 EXPANDED failures → cursor not advanced).
- **Parser proven against ground truth:** the tested core `parse_result_page()` runs offline against the real captured DOM (`recon/raw_html/02_result_list.html`, copied to `scaffold/tests/fixtures/clerk_recordings/result_list_real.html`) — all 50 rows extract cleanly. Test: `scaffold/tests/test_clerk_recordings.py`, 13 scenarios / 41 assertions, 0 failures.
- **Live-run dependency (operator action):** the portal is a React SPA, so live fetching uses Playwright headless Chromium, imported lazily so the module + tests load without it. To run live: `pip install playwright && python -m playwright install chromium`.
- **Config:** `sources.clerk_recordings.scraper_module` updated to `scrapers/clerk_recordings.py` (was the `clerk_seeded.py` placeholder) via the atomic writer; schema re-validated OK.
- **NOT yet done (blocks leads from this source):** the translator. The translator spec (`recon/publicsearch_clerk_recordings_translator_spec.md`) is v1 / AWAITING OPERATOR REVIEW with **7 unresolved open questions** (§15) — most consequentially (a) confirm the three-tuple output shape, (b) where the LEVEL-2 flags attach, (c) several TBD canonical field names, (d) unknown-doc-type handling, (e) address-less parcel fallback, and (f) **15 new GAP canonical doc types** that downstream matchers key on and that would need adding to `knowledge_base/domain/canonical_doc_types.json`. These need operator decisions before the translator is written; the scraper does not depend on them.
- **Environment:** `jsonschema` installed (was missing — had blocked 16 gate tests). On Windows, run the gate with `PYTHONUTF8=1` or 4 v5.4.0 tests crash on a `→` char under cp1252 (test logic passes; it's a console-encoding artifact in the test print statements, not a framework defect).

---

## 2026-05-28 — Build Mode: clerk_recordings translator built + wired (5 clean patterns)

- **2026-05-28 (Build Mode increment, cont.):** Built + wired the `publicsearch_clerk_recordings` translator (`scaffold/pipeline/translators/publicsearch_clerk_recordings.py`, registered built-in, county- and vendor-agnostic per §4.31). LEVEL 2 scope per the translator spec: config-driven `doc_type_code_map` code→canonical mapping, `field_map` application, the two LEVEL-2 flags (lifecycle suppression, fc-dedup), no LEVEL 3 inference. Test: `scaffold/tests/test_clerk_recordings_translator.py`, 6 scenarios / 23 assertions, 0 failures (includes a keystone end-to-end through `build_leads.run_pipeline`).
- **Decisive integration finding:** the v5.4.0 orchestrator (`build_leads._signal_to_raw_event`) **ignores the translator's `doc_type` canonical** and re-derives the registry canonical from the signal's *subtype label* via `normalize.normalize_doc_type` → `doc_type_bridge.monolith_to_registry`, **silently dropping** any signal whose label doesn't normalize to a registry type. Probed all 20 clerk codes: **only 6 survive today** (DECREE, FTL, LIS PEN, MECHLN, STL, HOSP LN) and DECREE is mis-mapped (umbrella → FINAL_DECREE_OF_DIVORCE). So this increment wires **5 clean patterns** (Lis Pendens, Mechanics Lien, Federal Tax Lien, State Tax Lien, Hospital Lien) — operator chose "5 clean patterns now, no framework-vocab change."
- **Proven end-to-end:** translator → orchestrator → §17–§20 → seam yields **5 scored leads, §20 DEPLOY_OK, 0 dropped** (pattern_counts {foreclosure:1, lien:2, tax:2}; 4 APPROVED_FOR_DASHBOARD, 1 REVIEW_REQUIRED for the address-less hospital lien). The 15 unsupported codes are skipped + counted by the translator (reason `unsupported_doc_type_pending_vocab`), NOT silently dropped.
- **Config (atomic writer, schema-valid):** `sources.clerk_recordings` now has `translator: publicsearch_clerk_recordings` (the schema-enumerated name), `parcel_id_prefix: BX-PS-`, `field_map {doc_number←document_number, address←property_address}`, and `translator_config` with the 5-code `doc_type_code_map`, `unknown_doc_type_handling: skip_with_warning`, plus forward-compat `foreclosure_dedup_codes`/`lifecycle_suppression_codes` (inert until those codes are added to the map).
- **DEFERRED (needs a framework-vocab decision):** to turn the high-value **Probate / Will / Letters / Foreclosure / Judgment** cluster (and the broad umbrellas) into leads, the universal `normalize.py` + `doc_type_bridge.py` (+ `canonical_doc_types.json`) must learn those labels, plus scoring calibration. That's a cross-county framework change, intentionally not done in this increment.
- **Still pending for full multi-source build:** clerk live run needs Playwright installed; court_civil/court_probate need recon→scraper-spec (Tyler Odyssey); tax_collector needs a delinquency-roll access decision.

---

## 2026-05-28 — Framework-vocabulary extension: probate/estate cluster

- **2026-05-28 (Build Mode increment, cont.):** Extended the UNIVERSAL framework vocabulary to unlock the clerk **probate/estate cluster** (codes PROBATE / WILL / LETTERS) as leads. Operator chose to extend the probate cluster (the clean subset); **Foreclosure (FC) and Judgment (JUDG) were deliberately held back** — FC duplicates `foreclosure_notices_map` and needs the dedup join (deferred); JUDG is an umbrella and not necessarily property-affecting (the §4 product rule + the translator spec defer property-affecting-judgment detection to LEVEL 3). Wiring either naively would inject duplicates/noise.
- **Three new canonical doc types** added to `knowledge_base/domain/canonical_doc_types.json` (registry now 77, was 74): `PROBATE_RECORDING`, `WILL_RECORDING`, `PROBATE_LETTERS` — all `lead_pattern: estate`, `source_class: lead_generating`. Their `common_abbreviations` ("PROBATE" / "WILL & TESTAMENT"/"WILL" / "LETTERS") auto-wire `normalize.normalize_doc_type` (no normalize.py code change). `doc_type_bridge.REGISTRY_TO_LEAD_TYPE` maps all three → the existing §16 "Probate" lead type (the 27-type §16 sweep is unchanged). Bridge totality test bumped 74→77.
- **§17 intentionally NOT touched:** the probate §17 rule uses DOCUMENT_BODY extraction, and the v1 clerk scraper captures only the result list (no document body), so these records route to `REVIEW_REQUIRED` either way. Adding them to the §17 probate fan-out would change no outcome but would force a §17 coverage-test edit (42→45). Left at 42. Umbrella probate recordings routing to human review (to identify the heir/estate contact) is the correct, honest behavior.
- **Config:** clerk `doc_type_code_map` now has 8 codes (5 liens + PROBATE/WILL/LETTERS). Proven end-to-end: probate cluster → 3 `estate`-pattern scored leads (lead_type "Probate"), §20 DEPLOY_OK, 0 dropped, all REVIEW_REQUIRED. Test `test_clerk_recordings_translator.py` now 7 scenarios / 26 assertions.
- **Gate:** universal 30/30 (bridge 77, §17 units still 42, golden path + county-agnostic regression clean); adapters/translator all green. NOTE: this is a cross-county framework-vocab change — a FRAMEWORK_VERSION bump is the maintainer's call (left untouched).

---

## 2026-05-28 — Build Mode: FIRST LIVE clerk_recordings run → combined dashboard (544 leads)

- **2026-05-28 (Build Mode increment — operator chose "clerk live run"):** Installed Playwright + Chromium and executed the first-ever **live** clerk_recordings pull against `bexar.tx.publicsearch.us`. `first_run_backfill --backfill-days 7` (window 2026-05-22→05-29), all 20 doc-type codes swept, **292 raw records** appended, run status `success`, cursor advanced. Raw breakdown: HOSP LN 80, JUDG 55, NOTICE 50, LIEN 31, PA 21, FTL 13, STL 11, LIS PEN 9, MECHLN 9, MEMO 7, DECREE 4, CSUP LN 2. (PROBATE/WILL/LETTERS returned 0 in this particular 7-day window — low frequency, not an error.)
- **BUG FOUND + FIXED (would have hard-halted EVERY live run):** `scrapers/clerk_recordings.py` `detect_hard_halt` matched the bare token `captcha`, which collides with the benign `"captcha-site-key":"6Lf..."` reCAPTCHA config embedded in *every* normal PublicSearch results page. The first probe false-halted on legitimate data (270KB page, `role="row"` present, title "Search Results - Bexar County..."). Fix: dropped the bare `captcha` alternative (kept `recaptcha`/`hcaptcha`/"verify you are human" + provider markers cloudflare/turnstile/datadome/etc.), and added a defense-in-depth guard — a page serving `role="row"` result rows is never a challenge page. Scraper unit tests still 13/13; the existing `reCAPTCHA challenge` detection test still passes.
- **Transport + parser validated on LIVE DOM:** single-page probe pulled 50 real Mechanics Lien rows, all fields clean (doc#, parties, address, legal desc).
- **parcel_master scraper hardened (latent footgun removed):** the scraper wrote FLAT records; the canonical translator requires the §4.32 `raw_payload` envelope (previously bridged by a one-time external transform — see 2026-05-15 Step 5''). Added `_wrap_record()` and applied it at both write sites so every re-pull is translator-ready. The existing per-source `field_map` is unchanged (it bridges the flat field names that now live inside `raw_payload`). Re-pulled targeted parcels (484 records matching 421 foreclosure addrs) and wrapped.
- **Combined production run (foreclosure + clerk + parcel-enrichment):** `lead_total = 544`, §20 `DEPLOY_OK`. `pattern_counts {foreclosure:417, lien:89, tax:39}`. **`lien:89` is entirely clerk-sourced** (MECHLN 9 + HOSP LN 80; foreclosure_notices never emits `lien`); clerk tax liens (FTL 13 + STL 11) sit in the `tax` bucket. Clerk leads carry the `BX-PS-` parcel_id_prefix, real address/owner, `APPROVED_FOR_DASHBOARD`. Clerk translator: 122 signals from 292 records, 170 unwired codes skipped (`unsupported_doc_type_pending_vocab`).
- **Foreclosure count moved 287→423 raw / 417 leads:** the foreclosure map shows the *current* month's notices; ~2 weeks elapsed since the 2026-05-14 snapshot, so more June notices accrued. The 287 figure was a frozen baseline (`data/leads.baseline.json`), not a permanent count. `data/raw/` is git-ignored, so the original foreclosure/parcel raw were transient and had to be re-pulled.
- **OUTPUT SEAM (operational, important):** the v5.4.0 staged pipeline writes `data/dashboard.json`, but the dashboard front-end (`dashboard/dashboard.js` `DATA_PATHS_PROD`) + config `dashboard_payload` still serve `data/leads.json`. Both files are the SAME `display_*` schema (dashboard.js was already cut over), so the publish step is a promote: `cp data/dashboard.json data/leads.json`. Did that — served `leads.json` now = 544. The permanent cutover (rename pipeline output to leads.json, or repoint consumers to dashboard.json) is a maintainer call; left as a documented manual promote for now.
- **build_label honesty fix (§4.16 gap wired):** the combined run flipped `build_label` to `FULL_BUILD` via the crude `<3 translator-sources` heuristic — which counts the parcel *enrichment* source and can't express PARTIAL_BUILD. That overstates completeness (court_civil/court_probate/tax_collector still unbuilt). Wired `build_leads.main()` to honor an explicit `county_config.dashboard.build_label` override (additive, guarded; no existing county sets it; county-agnostic scanner still clean). Set Bexar `dashboard.build_label = PARTIAL_BUILD` via the atomic writer (schema VALIDATED). Served dashboard now honestly reads `PARTIAL_BUILD`.
- **OBSERVATION — enrichment match rate low:** only 19/544 leads `ENRICHED` (attribute_counts {entity_owned:4}) despite 484 targeted parcels. The build_leads parcel matcher (`match_signals_to_parcels`, fuzzy address) linked far fewer than the parcel scraper's own address pull found — a pre-existing fuzzy-address-matching limitation already flagged in the 2026-05-14 parcel notes (double-space situs, street-token fallback, `address_match_uncertain`). Not introduced by this increment (parcel translator produced all 484 parcels fine). Flagged for a future matcher-quality pass; not blocking.
- **Gate after all edits:** universal 30/30, county-agnostic scanner clean, clerk scraper 13/13, clerk translator 7/7, foreclosure 9/9.
- **Still pending for full multi-source build:** court_civil/court_probate need recon→scraper-spec (Tyler Odyssey); tax_collector needs a delinquency-roll access decision (PIA vs per-parcel BCAD walk). clerk daily_refresh now runnable live (Playwright installed); a scheduled refresh is not yet configured.

---

## 2026-05-28 — Build-Mode recon: Tyler Odyssey court portal (court_civil + court_probate)

- **2026-05-28 (recon increment):** Live read-only Playwright fingerprint of the Bexar County Justice Portal (Tyler Odyssey) at `portal-txbexar.tylertech.cloud/Portal/`. Full findings + scraper-spec seed in `runs/bexar_tx/recon/tyler_odyssey_court_recon.md`; evidence in `_tyler_probe_findings.json`, `_tyler_dropdowns.json`, `_tyler_casetypes.json`, and captured DOM (`_tyler_01_landing.html`, `_tyler_portal_home_dashboard_29.html` Smart Search, `_tyler_portal_home_dashboard_26.html` Hearings).
- **One portal serves both sources** — Smart Search (`/Portal/Home/Dashboard/29`, form `frmSS` POST → `/Portal/SmartSearch/SmartSearch/SmartSearch`, server-rendered HTML, no JSON API). civil foreclosure = CourtLocation **District Clerk**; probate = CourtLocation **County Clerk**. CaseType has no granular pre-filter (only "All Offices Case Search"); case type is known only from result rows.
- **ACCESS BLOCKER (decisive):** Smart Search submit is **reCAPTCHA v2** (sitekey `6LfqmHkU…`) for ANONYMOUS users, but `Settings_CaptchaDisabledForAuthenticated=true` — a **free registered account disables the CAPTCHA**, making it automatable via a seeded session. Phase 0 packet under-stated this (was PUBLIC_SEARCH_ONLY, no blocker); both court packets updated 2026-05-28 → `public_access_status=CAPTCHA_PROTECTED`, `next_access_strategy=request_free_account`, blocker recorded (atomic writer, schema VALIDATED).
- **DAILY-REFRESH PROBLEM:** Tyler Smart Search has **no date-only query** — `FileDateStart/End` only refine a name/case/business search; you cannot list "all cases filed on date X". A discovery strategy must be chosen + validated with an account: (A) **Search Hearings** date listing (most promising; also CAPTCHA-gated), (B) blank-criteria + CourtLocation + date range (unknown if Bexar allows empty criteria — needs account to test), or (C) case-number enumeration (needs confirmed Bexar numbering format). This blocks the scraper spec.
- **LEAD-VALUE caveat:** civil foreclosure (Rule 736 / home-equity) cases likely reference the property; probate cases reference the **decedent/estate, not a property address** → probate-court leads are party-first and depend on the existing parcel/clerk owner-of-record join (same heir-hunting pattern as the parcel `estate` owner-name signal).
- **NEXT STEP (operator action):** register a free Bexar Justice Portal account, then a 2nd recon WITH the account to (a) confirm CAPTCHA-free submit, (b) test blank-criteria+date-range + Search Hearings date listing, (c) enumerate real civil/probate case-type strings + a sample detail page (property-address presence). That second recon yields the data to write the `court_*` scraper specs. Both sources are **WAITING_ON_ACCESS** until then.

---

## 2026-06-03 — Build Mode: first live daily_refresh + overlap idempotency fix → 592 leads

- **2026-06-03 (Build Mode increment — operator chose "fresh live pull + rebuild"):** First-ever `clerk_recordings --mode daily_refresh` live run after the 2026-05-28 backfill. Window 2026-05-26 → 2026-06-03 (3-day overlap cursor from last success 05-29), all 20 codes swept, status `success`, 0 retries, **543 records appended** (run `20260603T230621_daily_refresh`, 701s), cursor advanced to 2026-06-03. Live foreclosure ArcGIS re-pull: 423 records, **0 new / 423 SAME** (current-month map unchanged since the 5/28 snapshot). Raw files backed up first to `data/raw/_backup_20260603T180545/` (reversible).
- **FRAMEWORK BUG FOUND + FIXED (would have broken EVERY daily_refresh rebuild):** the clerk 3-day overlap legitimately re-scrapes the same recorded documents — the raw file went 292 → 835 lines = **620 unique docs + 215 exact re-scrape duplicates**. Each re-scrape produces an identical deterministic `raw_event_id`/`evidence_id`, and `evidence_ledger.build_evidence_ledger` (§08) hard-raises on a duplicate `evidence_id`. So the documented overlap-cursor design had never actually been run through a rebuild (the 5/28 combined run used single-backfill data with no overlap). Fix: added a by-`raw_event_id` idempotency guard in `build_leads.run_pipeline` (the signals→raw_events loop), keeping the first occurrence — mirrors the §19 aggregator's by-key idempotency one stage upstream, county-agnostic (dedup key is the framework's own deterministic id). Surfaced as `payload.deduped_overlap_signals`. This is a cross-county framework change; **FRAMEWORK_VERSION bump left to the maintainer.**
- **Rebuild result:** `lead_total = 592`, §20 `DEPLOY_OK`, `deduped_overlap_signals = 98` (overlap re-scrapes that reached the signal stage; the rest were already skipped as unsupported doc types). pattern_counts {foreclosure:430, lien:97, tax:66}; score_tier {Strong:394, Workable:80, Low:37, Archive:81}; enrichment {ENRICHED:27, UNENRICHED:565}. Promoted `dashboard.json → leads.json`. Gate suite green (66 v5.4.0 + golden path + county-agnostic regression + writer + registry, all PASS).
- **Delta vs the 2026-05-29 baseline (544):** +48 leads, entirely clerk-sourced (wider/newer window — JUDG 114, NOTICE 99, HOSP LN 81, LIEN 50, etc. vs the 7-day backfill). Foreclosure unchanged. build_label still PARTIAL_BUILD (court/tax sources remain unbuilt).

---

## 2026-06-03 — tax_collector access-path recon (RECON ONLY, no build)

Operator asked to scope the tax delinquency access paths. Live recon performed; both config `open_questions` resolved, `known_limitations` recorded. **No source_role change, no scraper built** — build decision deferred to operator.

- **Q1 — does ACT (bexar.acttax.com) expose a delinquent-only filter/list? → NO (confirmed live).** The portal is per-account lookup only (owner / business / address / account / CAD ID / fiduciary). No delinquent list, no status filter, no bulk export, no tax-sale listings. It is a customer-facing balance-lookup tool, not a records database.
- **Q2 — per-parcel BCAD walk vs PIA bulk? → Neither is the right primary.** Recon found a better, event-based source. Ranking:

  | Path | Yields | Quality | Access / effort | Verdict |
  |---|---|---|---|---|
  | **A. LGBS tax-sale list** (`taxsales.lgbs.com`) | Bexar tax-foreclosure properties: Sale / Resale / Struck-off / Future-sale | **P0/P1 event-based distress** (post-judgment tax foreclosures; aligns with the §4 product rule) | Free, public, no login. SPA + Google Maps, API-backed (endpoint surfaces on county search — Playwright fallback). MEDIUM, ~clerk-level | **Recommended primary** |
  | **B. PIA bulk delinquent roll** (open-records to Tax A-C) | Full delinquent account roll | Broad but weaker/earlier signal; **B-grade** (delayed) | Out-of-band request, possible fee, days–weeks latency, recurring | Optional breadth supplement |
  | **C. Per-parcel BCAD→ACT walk** | Per-account delinquent balance | — | ACT has no delinquent filter → discovery = blindly querying ~700k+ accounts. Infeasible | **Enrichment only**, never discovery |

- **Supporting facts:** Bexar's delinquent-tax attorney is Linebarger Goggan Blair & Sampson (LGBS); tax-foreclosure sales run the first Tuesday of the month at the courthouse west side. Struck-off/resale properties are also published as PDFs on bexar.org's Document Center ("Bexar County Resale Properties Distribution List"). The Tax A-C's "$10 Written Statement Regarding Delinquent Taxes" is a per-bidder request, not a bulk roll.
- **Recommendation (for the operator's build decision):** build **LGBS tax-sale (A)** as the primary tax-distress source; use **ACT (C) as targeted enrichment** (delinquent-balance confirmation on existing leads — consistent with the framework's tax-account = enrichment rule); hold **PIA (B)** as an optional breadth layer. If pursued, the honest config change is: reclassify ACT `tax_collector` → ENRICHMENT and add a new `tax_sale_lgbs` PRIMARY source (would go through the atomic writer + `operator_override_audit`). **Not done — awaiting operator go-ahead.**
- **Next Build-Mode step if approved:** live recon on `taxsales.lgbs.com` with a Bexar county search to capture the JSON API (or fingerprint the SPA for Playwright), then write the `tax_sale_lgbs` scraper spec.

---

## 2026-06-03 — Build-Mode recon: LGBS tax-sale API (tax_sale_lgbs) — READ-ONLY

- **2026-06-03 (recon increment — operator chose "proceed to LGBS Build recon"):** Live fingerprint of `taxsales.lgbs.com`. **Decisive finding: it is NOT just an SPA — there's a clean public Django-REST JSON API**, no auth, no CAPTCHA. The lead feed is `GET /api/property_sales/?county=BEXAR+COUNTY&state=TX&sale_type=SALE,RESALE,STRUCK%20OFF,FUTURE%20SALE&in_bbox=<bexar bounds>&ordering=precinct,sale_nbr,uid` — DRF-paginated (`count`/`next`/`limit`/`offset`), **84 active Bexar records** on 2026-06-03. Supporting endpoints: `/api/venues/suggest/` (Bexar venue id 1000000015 + bounds), `/api/sale_status/` (8 statuses), `/api/property_sales/cluster/`, `/api/filter_bar/`.
- **Record shape is near-ideal for a distress lead** (full field list + sample + lead-field mapping + 5 open questions in `recon/tax_sale_lgbs_recon.md`): `uid` (dedup), `cause_nbr` (tax-suit cause), `sale_date_only` (event date), `sale_type`, `prop_address_one/_city/_state/_zipcode`, `value` (adjudged) + `minimum_bid`, `account_nbr` (→ BCAD/ACT enrichment join), pre-geocoded `geometry`, `status` (lifecycle), `county_sale_list` (official PDF). parser_confidence high.
- **This confirms the scoping recommendation**: LGBS is a much easier and more honest primary tax-distress source than an ACT per-account walk (which can't discover delinquents) or a PIA roll. Effort is LOWER than the clerk build (clean API vs React SPA).
- **NOT done (needs operator go-ahead — crosses the build/vocab gates):** writing the `tax_sale_lgbs` scraper spec + scraper/translator; the config change (ACT `tax_collector` → ENRICHMENT, add `tax_sale_lgbs` PRIMARY via atomic writer + `operator_override_audit`); and a new tax-foreclosure canonical doc-type/pattern (cross-county vocab decision). See the 5 open questions in the recon file.
